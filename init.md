# init — 自作オーディオミドルウェア(Rust) 初期構築仕様

このドキュメントは **初期構築が完了した時点で削除**され、`CLAUDE.md` と `docs/` 以下に引き継がれる。
初期構築の間は、これが唯一の正となる仕様書。
(旧計画はクライアントテンプレートリポジトリ側に置かれていたが、本書へ全内容を移管・拡張済み)

## 凡例

- **【確定】** — 合意済み。勝手に変更しない。変更が必要と判断したら必ず確認を取ること。
- **【仮】** — 妥当な既定値として設定したもの。実装は進めてよいが、後から変わりうる前提で
  「1箇所を書き換えれば済む」構造(設定構造体 / 定数)にしておくこと。
- **【未定】** — 未決。実装フェーズで到達する前に確認を取ること。

本書で**テンプレート仕様「話題名」**と呼ぶものは、クライアントテンプレート
(`music-game-client-template`)の仕様書(初期構築後は `docs/spec/` / `docs/adr/` に置かれる本体仕様)を指す。
特定のファイル名には依存しない。

---

## 1. 概要

CRI Ware の大まかな代替となる、**音楽ゲーム向けオーディオミドルウェア**を Rust で自作する。
CRI の全機能の再現ではない。**音ゲーに必要な機能だけを絞って作る。**

### なぜ作るか 【確定】

テンプレート仕様「オーディオ抽象化」で明記したとおり、Unity 既定オーディオの弱点は2つ:

1. **タップ SE の再生開始遅延**(特に Android)。判定精度には影響しないが体感品質を落とす。
   CRI 採用の実質的な動機はここにある
2. **クロックの精度**。`AudioSettings.dspTime` は更新が離散的で、スムージングで補うしかない

このミドルウェアは上記2点を **OS のローレイテンシ音声 API を直接叩くことで根本解決**する。

### テンプレートとの関係 【確定】

- テンプレート側の受け口は `IAudioService` / `IMusicClock`(テンプレート仕様「オーディオ抽象化」)。
  ミドルウェアは **CRI と同格の「差し替え実装のひとつ」**であり、テンプレートの既定実装は
  あくまで `UnityAudioService` / `UnityMusicClock` のまま
- **本リポジトリはテンプレートのフェーズ進行とは独立に開発する。**
  依存関係は「テンプレート Phase 2 で `IAudioService` / `IMusicClock` の IF が確定してから
  アダプタ統合(M2 後半)に着手できる」の1点のみ。Rust 側(M0〜M2 前半)はいつでも始められる
- **アダプタの置き場所 【仮】**: テンプレート側の `App.Audio.Native` アセンブリ
  (テンプレートリポジトリに置く。`App.Audio.Cri` と同じ隔離方式 — CRI SDK が外部で
  アダプタがテンプレート側にあるのと同じ構図に揃える)。
  本リポジトリの UPM パッケージは**テンプレート非依存物のみ**
  (ネイティブバイナリ + 生成 C# バインディング + 薄い C# ラッパ `Mw.Native`)を含む。
  こうすることで、UPM 単体でも(テンプレート無しでも)コンパイル・検証できる

### 成功基準 【仮】

テンプレート仕様「デバッグ機能」の判定計測ハーネスと実測で検証する。

**基準端末【確定・2026-08-25】: iPhone 14 / iOS 26.6.1。**
テンプレート仕様の未決事項「プロファイル基準端末」もこれに揃える。
手元の2台(iPad 第8世代 / iPhone 14)のうち**ハードウェア出力レイテンシが不利な方**を選んだ
(iPad 5.0ms に対し iPhone 14 は 17.9ms)。緩い端末を基準にすると、厳しい端末で
目標を割っていることに気づけないため。

| 項目 | 目標 |
|---|---|
| SE トリガー → 出力 | **ミドルウェアの取り分 ≤ 10ms**(= コールバック待ち + I/O バッファ長)。かつ Unity 既定実装との A/B で有意に改善していること |
| クロック精度 | `SongTimeAt` 換算のジッタ **±1ms 以内**、単調増加。ドリフトは **フル尺曲で `|drift| ≤ 100ms`**(異常検出用の上限。理由は下記) |
| 安定性 | 通常プレイ1曲を通してアンダーラン 0 回 |
| 統合 | テンプレートのライブを VContainer の登録替えだけでネイティブ実装に切り替えて完走できる |

**ドリフトの条件を「なし」から上限値へ変えた理由【確定・2026-08-25】。**
実機計測(`docs/measurement-m1.md` §11)で -117ppm 程度のドリフトが出たが、
**この量は判定精度に影響しない**:

- ドリフトの正体は**音声ハードウェアの水晶とシステムタイマーの周波数差**で、
  100〜300ppm は普通に起こる
- 曲時間は「レンダリング済みフレーム数 ÷ サンプルレート」で作られるため、
  **譜面と音の関係はズレない**(どちらも「音の秒」で数えている)
- ズレるのは「タップ時刻(システム時計)→ 曲時間」の変換だけだが、
  **この相関は毎フレーム取り直している**ので累積しない

**判定精度を決めているのはジッタのほう**で、そちらは ±1ms のまま据え置く。
一方、桁違いのドリフトは**サンプルレートの取り違え**(44.1k を 48k と思い込む =
88000ppm)のような実装の異常を検出できるため、条件自体は上限値として残す。

**SE レイテンシの目標を「取り分」で定義し直した理由【確定・2026-08-25】。**
当初は「iOS ≤ 20ms」という end-to-end の絶対値だったが、実測の結果**この指標は
実装の良し悪しではなく端末の性質を測ってしまう**ことが分かった
(`docs/measurement-m1.md` §10.4)。同じミドルウェアが iPad 第8世代では 12.5ms で
目標を満たし、iPhone 14 では 25.4ms で満たさない。差は**ハードウェア出力レイテンシ
(5.0ms vs 17.9ms)**で、これは AVAudioSession が申告してくる値そのもの ——
**ミドルウェアが触れる層より下にあり、Unity 実装でも同じだけ払う**。

「取り分」= コールバック待ち + I/O バッファ長 で定義すると、両端末とも 7.5ms となり
端末をまたいで比較できる。end-to-end の絶対値が要るときは、この値に
`mw_get_output_latency_ns` の実測を足せば復元できる(情報は失われない)。

---

## 2. 決定事項サマリ

| # | 論点 | 決定 | 状態 |
|---|---|---|---|
| M1 | 実装言語 | **Rust**(stable)。コア・プラットフォーム層とも Rust で書き、OS 依存部のみ薄いシムを許容 | 【確定】 |
| M2 | 境界 | **C ABI(FFI)+ P/Invoke**。C# バインディングは **csbindgen** で自動生成 | 【仮】 |
| M3 | スレッドモデル | **リアルタイム音声スレッドはロック・アロケーション禁止**。ゲームスレッドとはロックフリーのコマンド/イベントキューで通信 | 【確定】 |
| M4 | コールバック | **C → C# のコールバックはしない**。イベントは C# 側が毎フレームポーリング(IL2CPP/Mono の逆呼び出しの複雑さを避ける) | 【確定】 |
| M5 | クロック | **音声スレッドのサンプルカウンタ + デバイスタイムスタンプの相関**を正とする(dspTime 方式より原理的に高精度) | 【確定】 |
| M6 | 出力バックエンド | 立ち上げは **cpal**(macOS Editor / iOS / Android を1系統で)。計測後、必要なら Android を **oboe 直叩き**、iOS を RemoteIO 直叩きに置換 | 【仮】 |
| M7 | デコード | **Symphonia**(wav / ogg vorbis)。楽曲は**メモリ上の圧縮データをストリーミングデコード**(ファイル IO を実行時に持ち込まない) | 【仮】 |
| M8 | 動作モード | **フルネイティブのみ**(BGM も SE もミドルウェア経由)。Unity 側 BGM + ネイティブ SE の混成モードは作らない — クロックの正が2つになるため | 【確定】 |
| M9 | 配布形態 | **UPM パッケージ**(git URL 参照)。**テンプレート非依存物のみ**を同梱(§1「テンプレートとの関係」)。`App.Audio.Native` アダプタはテンプレート側 | 【仮】 |
| M10 | 動画(Sofdec 相当) | **スコープ外**。`IVideoService` はテンプレート側で IF のみのまま | 【確定】 |
| M11 | リポジトリ | **`open-audioware`(本ディレクトリ)**。旧計画の未決事項(仮称 rhythm-audio)はこれで確定 | 【確定】 |
| M12 | 内部フォーマット | **f32 ステレオ固定でミックス**し、サンプルレートは出力デバイスに追従。素材は 48kHz を推奨し、不一致はリサンプルで吸収 | 【仮】 |
| M13 | 音量遷移の品質規約 | **すべての音量変化・停止はランプ(短いフェード)を通す**。ステップ変化によるクリックノイズを構造的に排除する | 【確定】 |
| M14 | 楽曲ボイス | **クロックを持つ楽曲ボイスは1本のみ**。M4 で BGM 用に<b>2本目</b>を足したが、<b>音楽クロックの発行元は1本目に固定</b>する(クロックの正を1つに保つ、という当初の理由はそのまま守られる)。BGM 側はクロックを持たず、ループ再生とフェードだけを扱う | 【確定】 |

---

## 3. スコープ

### 作るもの

- ローレイテンシの SE 再生(発音は次オーディオコールバック = 1バッファ以内)
- 楽曲のストリーミング再生: 予約再生(PlayScheduled 相当)/ ポーズ / 巻き戻し付き再開 / シーク / 停止
- サンプル精度の音楽クロックと、ホスト時刻(`Time.realtimeSinceStartup` 系)との相関 API
- バス(BGM / SE / Voice / Master): 音量、フェード
- サンプル精度のスケジュール発音(メトロノーム / キャリブレーション用クリック)
- 選曲プレビュー用のループ区間再生 + フェードイン/アウト
- 出力レイテンシの問い合わせ、出力ルート変化・アンダーラン・曲終了のイベント通知
- オフラインレンダリング(デバイス無しでバッファに書き出す)— テストと CI の土台
- 生成 C# バインディングと薄い C# ラッパ(`Mw.Native`)、UPM パッケージ
- 統合検証用の最小 Unity サンプルプロジェクト(テンプレート非依存)

### 作らないもの

- 動画再生(M10)
- DSP エフェクト(リバーブ等)、3D 空間音響 — 音ゲーコアに不要
- 録音、マイク入力(キャリブレーションの自動計測に将来使う可能性はあるが初期スコープ外)
- アセット暗号化(CRI にはあるが、テンプレート仕様「データ永続化」のチート対策水準に合わせて見送り)
- タイムストレッチ(ピッチ維持の速度変更)。譜面エディタの 0.5x 再生は
  当面リサンプリング(ピッチも下がる)で妥協し、必要になったら検討 【仮】
- ハプティクス — テンプレート側(Unity)の責務のまま
- `App.Audio.Cri` 的な他ミドルウェア対応 — テンプレート側の責務

---

## 4. 機能仕様

### 4.1 バスとミキシング 【仮】

- バスは **Master / BGM / SE / Voice の固定4本**(テンプレート仕様「オーディオ抽象化」の
  `IAudioService` のバス構成に整合)。任意本数のバスツリーは作らない — 音ゲーに不要
- 各バスは音量(linear。UI 側の dB 変換はテンプレート側の責務)とフェード
  (目標値 + 時間。ブロック内はサンプル単位の線形ランプ)を持つ
- ミックスは **f32 ステレオ**で行い、モノ素材は両チャネルへ等パワー展開(M12)
- **Master にソフトクリッパを置く**(同時発音が重なった際の折り返し歪み防止)。
  通常運用でクリッパが動作しないゲインステージングを既定とし、動作した場合は
  開発ビルドでイベント(§4.6)として観測できるようにする
- 音量の即時変更・停止も内部では短いランプ(【仮】5ms)を通す(M13)

### 4.2 SE 再生 【仮】

- SE はロード時に**全デコードして PCM でメモリ常駐**(§5.2)。発音要求は次のオーディオ
  コールバックで必ず発音される(= 遅延は1バッファ + 出力レイテンシのみ)
- **ボイスプール**: 同時発音数は既定 64。枯渇時は**最も古いボイスをスティール**する
  (音ゲーの SE は短く、体感上問題になりにくい方式を既定とする)
- 同一 SE の連打(タップ SE)は重ね発音を許可する。発音制限(同一 SE の同時数上限)は
  パラメータとして持つが既定は無制限
- 音量・バス指定は発音時パラメータ。再生中ボイスの操作(停止・音量変更)は
  ボイスハンドル経由で可能とする

### 4.3 楽曲再生 【確定】

- 楽曲は**圧縮データをメモリに保持し、専用デコードスレッドでストリーミングデコード**、
  リングバッファ経由で音声スレッドに供給する(実行時のファイル IO を持ち込まない)
- 対応操作: 準備(プリロール込み)/ 予約再生 / ポーズ / **巻き戻し付き再開**
  (テンプレート仕様「中断対応」の「数秒巻き戻し + カウントダウン再開」の受け皿)/
  シーク / 停止 / ループ区間設定(選曲プレビュー用。フェードイン/アウト付き)
- **予約再生** `mw_music_play_scheduled(host_time_ns)`: 指定ホスト時刻にサンプル境界で
  発音する(`PlayScheduled` 相当)。プリロール完了前の予約はエラーではなく
  「準備完了後、可能な最速時刻」へ繰り下げてイベントで通知する 【仮】
- 準備状態(ロード中 / 準備完了 / 再生中 / ポーズ中)は状態問い合わせ API で取得する
  (コールバックはしない。M4)

### 4.4 音楽クロック 【確定】 — このミドルウェアの心臓部

`IMusicClock` の品質を Unity 実装より一段上げるための設計。

- 音声スレッドが「楽曲ボイスをこれまで何フレーム(サンプル)レンダリングしたか」を数える
- 出力デバイスのタイムスタンプ API(AAudio `getTimestamp` / CoreAudio の `AudioTimeStamp`)で
  「いま DAC から出ているのはどのサンプルか」を取得し、**曲位置 × ホスト単調時刻**の相関点を持つ
- FFI は `mw_music_get_position(out snapshot)` でスナップショット
  `{ song_frames, host_time_ns, sample_rate, is_playing, generation }` を返す。
  C# 側はこれを基準に `SongTimeAt(t) = song_frames/rate + (t - host_time_ns)` で
  任意時刻の曲時間を線形外挿する
- **generation(世代カウンタ)**: シーク・停止・再オープン(ルート変化)で不連続が起きるたびに
  インクリメントする。C# 側は世代を跨ぐ外挿・補間をしてはならない(不連続を跨いだ
  なめらか補間は判定時刻を歪めるため)。単調性の保証は同一世代内でのみ与える
- スナップショットは seqlock 相当の atomic 読み書きで、どのスレッドからも
  ロック無しで取得できるようにする
- ホスト単調時刻(`CLOCK_MONOTONIC` / `mach_absolute_time`)と Unity の
  `Time.realtimeSinceStartupAsDouble` の原点差は、C# 側が起動時に両方をサンプリングして
  確定させる(以後は定数オフセット)

### 4.5 スケジュール発音 【確定】

- `mw_se_schedule(id, bus, host_time_ns)` でサンプル精度の予約発音を行う。
  用途はメトロノームとキャリブレーション用クリック
  (テンプレート仕様「プレイヤー設定」のキャリブレーション画面の音源)
- 予約はソート済みキューで保持し、該当バッファのレンダリング時に**バッファ内オフセット
  サンプル位置**から発音する(バッファ境界への丸めはしない — 丸めるとキャリブレーションの
  計測値そのものが汚れる)

### 4.6 イベント通知 【確定】

- 種別 【仮】: `RouteChanged` / `Underrun` / `MusicEnded` / `MusicLooped` /
  `StreamError` / `ClipperEngaged`(開発ビルドのみ)
- C# 側が毎フレーム `mw_poll_events(buf, cap)` でポーリングする(M4)。
  呼び出し側が確保したバッファに blittable 構造体で書き込み、GC アロケーションゼロ
- イベントキューは固定容量(【仮】64)。溢れた場合は**古いものから破棄**し、
  破棄数をカウンタとして次のポーリングで報告する(黙って落とさない)

### 4.7 デコードとリサンプリング 【仮】

- コーデックは **wav(PCM)/ ogg vorbis**(Symphonia)。opus は必要になったら追加(MU2)
- SE はロード時に全デコード + 必要ならロード時にリサンプルして出力レート化(再生時コストゼロ)
- 楽曲はストリーミングデコードし、レート不一致はデコードスレッド上でリサンプル
  (リサンプラは **rubato** 【仮】)。音声スレッドにはリサンプル済み PCM だけが渡る
- ogg のシークはサンプル精度でないため、**シーク後にデコード読み捨てでサンプル境界へ
  合わせる**。精度はゴールデンテスト(§8)で固定化する
- 素材制作の推奨レートは 48kHz(モバイルの主流出力レート。リサンプル頻度を最小化する)

### 4.8 エラーモデルと ABI 【確定】

- 全 FFI 関数は**エラーコード(負の整数)返し**。列挙は `MwResult` として一元管理し、
  csbindgen で C# 側 enum も自動生成する
- ハンドルは不透明な整数 ID(ポインタを C# に渡さない)。二重解放・使用後解放は
  無効 ID エラーとして検出する(クラッシュさせない)
- `mw_abi_version()` で ABI バージョンを返し、C# 側が起動時に期待値と照合する。
  **ABI 互換の破壊は semver メジャーバージョンでのみ許可**
- ログ: 開発ビルドでは stderr / logcat / os_log へ出力。リリースビルドでは
  重要イベントのみ(イベントキュー経由で C# 側からも観測可能)【仮】

---

## 5. アーキテクチャ

### 5.1 クレート構成 【仮】

cargo workspace で3クレートに分ける。

```
crates/
  mw-core/       … ミキサ、ボイス管理、クロック、デコード、リサンプル。OS 非依存・デバイス非依存。
                    オフラインレンダリングはここだけで完結する(テストの主戦場)
  mw-backend/    … 出力デバイス抽象(Backend trait)と cpal 実装。
                    将来の oboe / RemoteIO 直叩き実装もここに並べる
  mw-ffi/        … C ABI 境界。ハンドル管理、コマンド/イベントキュー、csbindgen の入力
```

- 依存の向きは `mw-ffi → mw-core / mw-backend`、`mw-backend → mw-core` のみ。
  mw-core は他クレートに依存しない(テンプレート仕様のレイヤ思想と同じ「内向き依存」)

### 5.2 スレッドモデル 【確定】

```
[ゲームスレッド (C#)]
   │  FFI 呼び出し(全て非ブロッキング。内部はコマンドをキューに積むだけ)
   ▼
[コマンドキュー (SPSC ロックフリー)]        [イベントキュー (SPSC)]
   │                                          ▲  RouteChanged / Underrun / MusicEnded / StreamError
   ▼                                          │
[音声スレッド(OS のオーディオコールバック)]──┘
   … コマンド消化 → デコード済みリングバッファからミックス → 出力。
     ロック・アロケーション・ブロッキング IO 禁止
   ▲
[デコードスレッド]
   … 楽曲のストリーミングデコード(Symphonia)。音声スレッドへはリングバッファで供給。
     シーク時はここで再位置決めして詰め直す
```

- SE はロード時に全デコードしてメモリ常駐(PCM)。楽曲のみストリーミングデコード
- ロード(デコード)はゲームスレッドをブロックしない。完了は状態問い合わせで検知

### 5.3 リアルタイム安全性の規約 【確定】

音声スレッド(オーディオコールバック)内で禁止するもの:

- ロック取得(mutex / rwlock)、チャネルのブロッキング受信
- ヒープアロケーション / デアロケーション(`Vec::push` の暗黙の再確保を含む)
- ファイル / ネットワーク IO、システムコール一般、`println!` 系
- パニック経路(`unwrap` / `expect` / 添字パニック)。コールバックは panic 境界で保護し、
  万一のパニックは無音出力 + `StreamError` イベントに落とす

検証手段: デバッグビルドでコールバック内アロケーションを検出するカスタムアロケータフックを
仕込む 【仮】。加えてオフラインレンダリングのテストでコールバック相当経路を常時実行する。

### 5.4 FFI 設計原則 【確定】

- 全関数スレッドセーフ・非ブロッキング・エラーコード返し(§4.8)
- 毎フレーム呼ぶ関数(クロック取得・イベントポーリング)は **GC アロケーションゼロ**
  (blittable 構造体 + 呼び出し側バッファ)
- C# バインディングは csbindgen で Rust から自動生成し、手書きの宣言ズレを構造的に排除する(M2)
- シンボルは `mw_` プレフィックスで統一

### 5.5 主要 API の概形 【仮】

```
mw_init(config) / mw_shutdown() / mw_abi_version()
  // config: 希望バッファ長、ボイス数、リングバッファ秒数 等。init/shutdown は冪等

mw_sound_load(bytes, len, mode, out_id)  // mode: SE(全デコード常駐) / Music(圧縮のまま保持)
mw_sound_release(id)

mw_se_play(id, bus, volume) -> voice     // 即時発音(次コールバック)
mw_se_schedule(id, bus, host_time_ns)    // サンプル精度の予約発音(メトロノーム用)
mw_voice_stop(voice) / mw_voice_set_volume(voice, vol)

mw_music_set(id)                         // ストリーミング準備(プリロール込み)
mw_music_state() -> state                // Loading / Ready / Playing / Paused
mw_music_play_scheduled(host_time_ns)
mw_music_pause() / mw_music_resume_at(frames)   // 巻き戻し付き再開(中断対応)
mw_music_seek(frames) / mw_music_stop()
mw_music_set_loop(begin_frames, end_frames)     // プレビュー区間ループ
mw_music_get_position(out snapshot)             // { song_frames, host_time_ns, rate, is_playing, generation }

mw_bus_set_volume(bus, vol) / mw_bus_fade(bus, target, ms)
mw_get_output_latency_ns(out ns)
mw_poll_events(buf, cap) -> count
```

---

## 6. テンプレートとの連携ポイント 【確定】

ミドルウェア側だけでは閉じない項目。テンプレート側に必要な受け口を明記する。

- **音源アセットの二形態**: Unity 実装は `AudioClip`、ネイティブ実装は**生バイト**
  (wav / ogg)を読む。楽曲カタログの「音源アドレス」は実装ごとに解決を変えられる必要がある。
  テンプレートのサンプル音源は生成スクリプト製(テンプレート仕様「サンプル音源」)なので、
  **生成時に AudioClip 用 wav と `.bytes` の両方を出力**すれば二重管理にならない
- **Unity Audio の無効化 【仮】**: ネイティブモードでは Project Settings の Disable Unity Audio を
  有効にし、全ての音をミドルウェアに通す(M8)。この設定切替はテンプレートのビルド設定側に持つ
- **ルート変化イベントの経路**: テンプレートは `AudioSettings.OnAudioConfigurationChanged` を
  使っている(テンプレート仕様「プレイヤー設定」)が、Unity Audio 無効時にこのイベントが
  機能するかは検証項目。機能しない場合に備え、ミドルウェアの `RouteChanged` イベントを
  `IAudioService` 側の通知に正規化して流す — **テンプレートのオフセット自動適用が
  どちらの実装でも同じ経路で動く**ようにする
- **譜面エディタは Unity 実装のままでよい 【仮】**: 制作ツールに低遅延は不要。
  波形表示も `AudioClip` ベースの既定実装をそのまま使う
- **アプリライフサイクル**: バックグラウンド移行・復帰・オーディオ割り込み(着信)は
  C# 側から `mw_music_pause` 等を明示的に呼ぶ(テンプレート仕様「中断対応」に従属し、
  ミドルウェアが勝手にポーズしない)。ただしストリーム自体の切断(AAudio の disconnect 等)は
  ミドルウェア内部で再オープンし、イベントで通知する
- **Unity Editor のドメインリロード**: init/shutdown を冪等にし、C# 側でドメインアンロード時に
  確実に shutdown する。dylib は常駐前提で設計する

---

## 7. 技術スタック

### 7.1 Rust ツールチェーン・主要クレート 【仮】

| 項目 | 選定 | 用途・備考 |
|---|---|---|
| Rust | stable(最新)、edition 2024 | MSRV は「最新 stable」で開始し、公開時に固定を検討 |
| cpal | 出力バックエンド | M6。全ターゲットを1系統で |
| symphonia | デコード(wav / ogg vorbis) | M7 |
| rubato | リサンプリング | §4.7 |
| rtrb | SPSC ロックフリーリングバッファ | コマンド / イベント / PCM 供給 |
| csbindgen | C# バインディング生成 | M2。IL2CPP 実績あり |
| cargo-ndk | Android ビルド | .so 生成 |
| criterion | ベンチマーク(任意) | ミキサのブロック処理性能の回帰検知 |

- lint / 整形: `cargo fmt` + `cargo clippy -D warnings`
- **cargo-deny** でライセンスと脆弱性アドバイザリを CI 検査する
  (テンプレートが権利的クリーンさを重視するのと同じ思想。同梱物のライセンスを機械的に保証)
- unsafe は FFI 境界(mw-ffi)と明示的に正当化できる箇所のみ。`#![deny(unsafe_op_in_unsafe_fn)]`

### 7.2 リポジトリ構成 【仮】

```
open-audioware/
  crates/
    mw-core/
    mw-backend/
    mw-ffi/
  unity/                  ← UPM パッケージ(バイナリ、生成バインディング、Mw.Native ラッパ)
  unity-sample/           ← 統合検証用の最小 Unity プロジェクト(unity/ をローカル参照)
  tools/                  ← パッケージ組み立て・バインディング生成の補助スクリプト
  docs/
  Makefile
  Cargo.toml              ← workspace
```

### 7.3 Makefile 【確定】

```
make setup            # ツールチェーン・ターゲット・cargo-ndk 等の導入確認
make lint / format    # fmt + clippy + cargo-deny / 自動整形
make test             # cargo test(mw-core のオフラインレンダリングが主)
make bench            # criterion(任意)
make bindgen          # csbindgen で C# バインディング生成
make build-macos      # .dylib(Unity Editor 用)
make build-ios        # .a → xcframework
make build-android    # .so(cargo-ndk)
make package          # UPM パッケージ組み立て(バイナリ + バインディング + ラッパ)
```

---

## 8. テスト戦略 【確定】

- **オフラインレンダリングが主戦場**。mw-core をデバイス無しで駆動し、出力バッファを検証する:
  - ミキサ・音量・フェードの数値検証、ソフトクリッパの動作検証
  - **スケジュール精度**: 予約発音が指定サンプル位置ちょうどに乗っているかをサンプル単位で検証
    (バッファ内オフセット発音 §4.5 の検証を含む)
  - **クロック検証**: レンダリング済みフレーム数とスナップショットの整合、
    シーク・ポーズ・世代カウンタ跨ぎの単調性規約(§4.4)の検証
  - デコード正当性: 既知波形(テンプレートの生成音源と同系のシンセ波形)をエンコード → デコードし、
    位相・長さ・ゲインを許容誤差内で照合する**ゴールデンテスト**。ogg シーク精度もここで固定化。
    ゴールデン波形はテスト時にコードで生成し、バイナリはコミットしない(権利面・LFS 不要化)
  - リアルタイム安全性: デバッグアロケータフックによるコールバック内アロケーション検出(§5.3)
- **実機計測**: 成功基準(§1)の実測。SE 遅延は
  1. まず**外部録音**(別端末でタップ音と出力音を録り波形間隔を測る素朴な方法)で
     ミドルウェア単体を計測(M1。テンプレートに依存しない)
  2. テンプレート統合後は判定計測ハーネス(テンプレート仕様「デバッグ機能」)で計測(M2 以降)
- **Unity 統合テスト**: テンプレート側 PlayMode テストのオートプレイ通し
  (テンプレート仕様「品質管理」)をネイティブ実装差し替えで1本流す
- **契約的テスト**: `IMusicClock` / `IAudioService` の振る舞いテストを Unity 実装と共通化し、
  両実装に同じスイートを流す(テンプレート仕様の契約テストスイートと同じ思想。
  スイート自体はテンプレート側にあり、アダプタ実装がそれに合格する)

---

## 9. ビルド・配布・CI

### ビルドターゲット 【仮】

| ターゲット | 形態 | 用途 |
|---|---|---|
| aarch64-apple-darwin | .dylib | Unity Editor(開発の主環境) |
| aarch64-apple-ios | .a → xcframework | iOS 実機(静的リンク) |
| aarch64-linux-android | .so(cargo-ndk) | Android 実機 |
| x86_64 系 | 任意 | CI テスト実行用。Windows Editor 対応は当面見送り 【仮】 |

### 配布 【仮】

- UPM パッケージ(M9): `Plugins/` 配下にプラットフォーム別バイナリ、生成済み C# バインディング、
  `Mw.Native` ラッパ、最小サンプルを含める。パッケージ名 / 組織 ID は 【未定】(MU7)
- バージョニングは semver。**ABI 互換の破壊はメジャーバージョンでのみ許可**し、
  `mw_abi_version()` で実行時検証する
- リリースは GitHub Actions でタグ駆動ビルド(macOS ランナーで dylib / xcframework、
  Linux ランナーで Android .so)→ パッケージに固めて Release 添付

### CI 【仮】

- `cargo fmt --check` / `cargo clippy -D warnings` / `cargo deny check` / `cargo test`
  (mw-core はオフラインレンダリングでデバイス無しテスト可能)
- ターゲット4種のクロスビルドが通ることを常時検証(バイナリ配布はタグ時のみ)

---

## 10. 実装フェーズ

各フェーズ終端で「ビルドが通り、テストが緑」を保つ(テンプレートと同じ運用)。
**各フェーズは独立して中断可能**にする。

| Phase | 内容 | 終了条件 | テンプレートへの依存 |
|---|---|---|---|
| **M0. 基盤** | git init / GitHub リモート / workspace 構成 / CI / cargo-ndk・xcframework のビルドパイプライン / FFI スケルトン + csbindgen / Makefile / unity-sample から `mw_init` → 無音ストリーム再生 | 4ターゲットのビルドが CI で通り、macOS Editor 上で init/shutdown が動く | なし |
| **M1. SE 再生** ★価値の核 ✅**完了** | ミキサ、ボイスプール、バス音量、ソフトクリッパ、wav ロード、即時発音、オフラインレンダリングとテスト群 | Editor と実機で SE が鳴る。**実機で Unity 実装との遅延 A/B を外部録音で計測し、目標値(§1)を満たす**。満たさない場合はここで M6 のバックエンド直叩き置換を判断 | なし(計測は外部録音方式) |
| **M2. 楽曲とクロック** ✅**完了(2026-08-25)** | Symphonia ストリーミング、リサンプル、予約再生、ポーズ/巻き戻し再開/シーク、サンプルカウンタ + タイムスタンプ相関、世代カウンタ。後半: テンプレート側 `App.Audio.Native` アダプタ実装と統合 | テンプレートのライブをネイティブ実装で完走でき、判定計測ハーネスのクロック計測(ジッタ・ドリフト)が目標値を満たす → **達成**(iPhone 14 実機でジッタ 0.402ms / 単調性違反 0 / アンダーラン 0。`docs/measurement-m1.md` §11) | **後半のみ**: テンプレート Phase 2(IF 確定 + ハーネス) |
| **M3. 実運用耐性** | ルート変化(イヤホン抜け / BT)検知と再オープン、割り込み処理、出力レイテンシ問い合わせ、アンダーラン検知・テレメトリ、iOS AVAudioSession シム(最小の Obj-C) | ルート切替・着信・バックグラウンドを跨いでもクラッシュ・無音化せず、テンプレートの自動ポーズ・オフセット切替が動く | テンプレート Phase 2(中断対応の実装) |
| **M4. 仕上げ機能** | フェード、プレビューループ、メトロノーム予約発音(キャリブレーション連携)、エディタ用の再生速度変更(リサンプル方式) | テンプレートの選曲プレビュー・キャリブレーション画面がネイティブ実装で動く | テンプレート Phase 6(キャリブレーション画面) |
| **M5. ハードニング** | 実機プロファイル(CPU / 電力)、長時間試験、UPM パッケージ仕上げ、リリース自動化、ドキュメント(統合手順・API リファレンス・設計 ADR) | ドキュメントだけ読んでテンプレート導出プロジェクトに導入できる | なし |

**M1 の A/B 計測が最重要のゲート。** ここで Unity 実装に対する優位が出なければ、
このミドルウェアの存在意義(§1)が崩れるため、先に進まず原因を潰すか計画を見直す。

---

## 11. エージェント運用 【確定】

- **計画は上位モデル(Fable / Opus)が行い、実装は下位モデル(Sonnet)が行う**
- 並列化して効率が上がる作業は、サブエージェントを並列で動かす
- **`.env` 等の秘密情報を出力しない。** 明示的な許可があった場合のみ読み取り・使用を許可する
- 作業の区切りごとに git コミットする
- **動作確認はまとめて後で行う。実装中にユーザーの手を止めない。** 例外は実機系のみ:
  1. **M1 の遅延 A/B 計測** — iPhone / Android 各1台への実機インストールと外部録音。
     iOS 署名は Apple Developer 登録済みのため自動署名で進められる(端末接続はユーザー作業)
  2. **M3 の実機挙動確認** — イヤホン抜き差し・BT 切替・着信・バックグラウンドの手動操作
- 【仮】/【未定】の項目に実装が到達したら、勝手に確定させず確認を取る

---

## 12. ドキュメント 【確定】

- **`README.md`** — プロジェクト概要、セットアップ手順、Makefile コマンド一覧
- **`CLAUDE.md`** — エージェント向けの運用ルールと、各ドキュメントへの索引(短く保つ)
- **`crates/<name>/CLAUDE.md`** — 各クレートの設計・役割・不変条件
  (特に mw-core のリアルタイム安全性規約 §5.3 はここに常設する)
- **`docs/`** — 肥大化する内容の切り出し先。統合手順・API リファレンス・設計 ADR(M5)

---

## 13. Git 運用 【確定】

- ローカルに git リポジトリを作成し、初期構築を `main` にコミットする
- **リモート: GitHub リポジトリ**(作業ディレクトリが外付けドライブ上のため、バックアップを兼ねる)。
  ⚠️ 初期構築時は private で作ったが、**2026-09-11 に public へ変更する決定をした**(MU4)
- ブランチ戦略: `main` は常にビルドが通る状態 / `feature/<phase>-<内容>` で作業 /
  マージ前にローカルで `make lint` `make test` / フェーズ完了時にタグ(`phase-m0` 等)
- Git LFS は使わない — バイナリ成果物(ビルド物・ゴールデン波形)はコミットしない方針のため(§8)

---

## 14. リスクと対応

| リスク | 対応 |
|---|---|
| cpal では遅延目標に届かない(特に Android の performance mode / exclusive 制御) | M6 の方針どおり oboe 直叩きへ置換。Backend trait で差し替え可能にしてある |
| AAudio はルート変化でストリームが死ぬ(disconnect) | 音声スレッド外で再オープン → クロック相関を取り直す処理を M3 で必須実装。再オープン中の曲位置は内部カウンタで継続し、世代カウンタを進める |
| Bluetooth の遅延が大きく・動的 | ミドルウェアでも吸収しきれない(コーデック依存)。テンプレート側の方針(ルート別オフセット + BT 警告、テンプレート仕様「プレイヤー設定」)に従い、こちらは正確な遅延報告に徹する |
| Unity Editor のドメインリロードでネイティブ状態が残留 | init/shutdown を冪等にし、C# 側でドメインアンロード時に確実に shutdown。dylib は常駐前提で設計 |
| iOS の AVAudioSession 設定(カテゴリ・バッファ長)は Rust から直接触れない | 最小の Obj-C シム(数十行)を xcframework に同梱。M3 で実装 |
| Unity Audio 無効化で `OnAudioConfigurationChanged` が死ぬ可能性 | §6 のとおりミドルウェア側イベントに正規化して一本化。M3 で検証 |
| Symphonia の ogg シークがサンプル精度でない | シーク後にデコード読み捨てでサンプル境界へ合わせる(一般的な手法)。ゴールデンテストで精度を固定化 |
| IL2CPP / Mono 両対応の P/Invoke 差異 | コールバック不使用(M4)+ blittable 型限定でリスクの大半を回避。csbindgen は IL2CPP 実績あり |
| アダプタがテンプレート側にあることによる分裂(バインディング更新とアダプタ更新のズレ) | ABI バージョン検査(§4.8)で実行時に検出。破壊的変更はメジャーバージョン + テンプレート側アダプタの追随を同一作業で行う |

---

## 15. 未決事項の一覧

実装がここに到達する前に確認を取ること。

| # | 項目 | 現在の扱い |
|---|---|---|
| MU1 | リポジトリ名・作業ディレクトリ | **確定**: `open-audioware`(本ディレクトリ。M11) |
| MU2 | 楽曲コーデック | ogg vorbis 【仮】。opus は必要になったら libopus FFI で追加 |
| MU3 | Windows Editor 対応 | 当面見送り 【仮】(開発環境が macOS のため。Backend trait 上は追加可能) |
| MU4 | ライセンス / 公開範囲 | **確定**(2026-09-11 ユーザー決定): ライセンスは MIT No Attribution(`LICENSE`)、**リポジトリは public**。⚠️ public 化にあたり、秘密情報・内部識別子が履歴に無いことを確認済み(gitleaks 全コミット / AWS アカウントID / 内部ホスト / private への依存) |
| MU5 | エディタ 0.5x 再生のタイムストレッチ化 | リサンプル方式で開始し、制作現場の要望が出たら検討 【仮】 |
| MU6 | マイク入力による自動キャリブレーション | スコープ外。M5 完了後の拡張候補としてメモのみ |
| MU7 | UPM パッケージ名 / 組織 ID | 【未定】。M5 のパッケージ仕上げまでに決定 |
| MU8 | 同時発音数・バッファ長等の既定値 | 【仮】の既定値(ボイス 64 等)で実装し、`mw_init(config)` で差し替え可能にする |

---

## 16. その他 【確定】

- 初期構築が完了した場合、本書を削除し、`CLAUDE.md` と `docs/` で集約管理すること
  (クライアントテンプレート側に残る旧計画の案内ファイルも同時に削除してよい)
