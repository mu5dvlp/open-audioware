# 実装の経緯

マイルストーン(M0〜M5)の定義と終了条件は初期構築仕様『§10 実装フェーズ』が正
([`spec/roadmap.md`](spec/roadmap.md))。
計測結果は [`measurement-m1.md`](measurement-m1.md) が正。
本ドキュメントは<b>いつ・なぜそうしたか</b>の記録。

> 元は作業ディレクトリ直下の `HANDOFF.md` に溜めていたが、再開時に読む文書として
> 肥大化したため 2026-08-26 にここへ移した。

## 到達状況(2026-08-26)

| Milestone | 状態 |
|---|---|
| M0 基盤 / M1 SE 再生 / M2 楽曲とクロック | **完了** |
| M3 実運用耐性 | **完了**(iOS の割り込み・バックグラウンド復帰・ルート変化〔OldDeviceUnavailable〕・アンダーラン検知テレメトリに続き、Android の AAudio disconnect 再オープン〔案A、ミドルウェア内部で再オープン〕を実装した。✅ **2026-09-04 に Android 実機で確認できた** —— 有線イヤホンをプレイ中に引き抜くと約0.5秒の無音を挟んでスピーカーへ復帰し、**音とノーツが一緒に戻る**〔= `restore_music` の位置復元が効いている〕ことをユーザーが確認。無音時間はユーザー判断で許容。Bluetooth 切断も同日に確認〔停止なくスムーズに切り替わり〕。⚠️ ただし無音ゼロは「Android がルート変更だけで処理し再オープンが走っていない」可能性があり、**「BT でも再オープンが動いた」とは書けない**——BT 経路で再オープンが要る状況が出たら、まず切断イベントの有無から確認すること) |
| M4 仕上げ機能 | **着手中**(M4-1 予約発音の C# ラッパ 完了 / M4-2 キャリブレーション連携 完了 / M4-3 BGM のネイティブ化 完了 / 選曲プレビューのネイティブ化: open-audioware 側の口〔既存の `mw_music_*`〕は検証・回帰テスト済みで完了。初期構築仕様『§10 実装フェーズ』の終了条件「テンプレートの選曲プレビュー画面がネイティブ実装で動く」はクライアント側の配線待ちのため、M4 全体としては未完了) |
| M5 ハードニング | 未着手 |

⚠️ **M3 を飛ばして M4 を進めている。** テンプレート側から `AudioSource` を剥がすのに
必要な機能(キャリブレーション・BGM・プレビュー)が M4 に集まっているため、
ユーザー方針「AudioSource には頼らず全面 middleware へ移行したい」に沿って順序を入れ替えた。

## 作業記録

作業記録の本体は日付ごとに [`docs/history/`](history/) へ分割してある(新しいものから順)。
**エントリを追記するときは、いちばん新しいファイルの先頭へ足し、下の索引にも1行足すこと。**
1ファイルが 300 行を大きく超えたら、新しい日付レンジのファイルへ分ける。

### [2026-09-23](history/12-2026-09-20.md)

- 🔴 **CI のカバレッジを 81.66% → 98.2% にした**(ユーザー指示)。眼目は
  **仮想オーディオデバイス**(`test_backend::VirtualBackend`)—— `Renderer::render` を
  実ホスト時刻つきで回すので、デバイスの無い CI でも `ffi.rs` の統合テスト一式
  (約830行)が走る(**43.24% → 98.64%**)。残りはエラー型の `Display` を全バリアント
  固定(実機で失敗理由を知る唯一の手段)。**90% を切るファイルはゼロ**、未到達行 1145 → 148。
  🔴 **薄いテストは足していない**(死んだ API・到達不能な防御的分岐は意図的に残した)。
  README にバッジ6本

- 🔴 **`Instance.backend` を `Box<dyn Backend + Send>` へ**(テストダブルを差せる形に)。
  動機は数字ではなく、**P3-11 の `teardown_orphaned_reopen`(段2に `mw_shutdown` が
  割り込む経路)が一度もテストされていなかったこと**。テスト4本を足し、
  **わざと3種類壊して落ちることまで実測**。`cargo test` 320 → **324 passed**。
  🔴 **CI(Codecov 実測)で `handle.rs` 13.43% → 70.22% / 全体 81.66% → 85.63%**
  (手元は 83.04% → 90.89%)。⚠️ CI にダミー音声デバイスを載せる道は閉じている

- 🔴 **P3-11(リファクタ計画 最後の1件)完了**: `maybe_reopen` の内部再オープンを
  ロック保持のまま行わない3段構成(段1切り離し/段2ワーカースレッド/段3差し込み)へ分割。
  `docs/plans/REFACTOR-PLAN.md` 39項目中 39件完了(100%)。⚠️ 受け入れ確認は Android 実機が要る

- 🆕 **CI に Codecov を入れた**(2026-09-23)—— `cargo test` を `cargo llvm-cov` へ置き換え、
  `patch 70%` をゲートに。🔴 実デバイスが要る `cpal_backend.rs` / `ios_interruption.rs` は
  `ignore`(除外しないと全体が恒常的に赤く見える)。実測で CI は +29秒(1分58秒 → 2分27秒)

### [2026-09-20](history/12-2026-09-20.md)

- 📝 **ADR-0001(`docs/adr/` 新設)**: セキュリティ枠組みの採用決定のミドルウェアへの当てはめ
- 🔧 **ネイティブ再ビルド**: P3-12 / P3-13 を `unity/Runtime/Plugins/` へ反映
  (cargo test 全緑 → macOS dylib + iOS xcframework。client 側スモークも緑)

### [2026-09-15(セッション42)](history/11-2026-09-15.md)

- **リファクタ Phase 3 の残り4件のうち3件を片付けた**(P3-8 / P3-12 / P3-13)。残るのは **P3-11 だけ**
- 🔴 **P3-8 で分かったいちばん大事なこと: ABI のズレ方は2種類あり、検査も2つ要る。**
  フィールド名・型のズレは C# のコンパイル(`make csharp-check`。Unity 非依存の `dotnet build`)、
  **フィールドの並べ替え**は `offset_of!` assert。⚠️ **並べ替えは `dotnet build` をエラー0 で通り、
  サイズ assert も通る** —— C# は黙って隣のバイトを読む(実測で確認)
- P3-12: 予約 SE のオーバーフロー件数を FFI へ(`mw_se_schedule` は捨てられても `Ok` を返すので、
  読む口が無い限り誰も気付けなかった)。`MwOutputUnderrunStats` は 24 → **32 バイト**
- P3-13: `SymphoniaDecoder::open_shared` を足して曲切り替えの数十MB の `memcpy` を無くした。
  🔴 `remove_music_bytes` の「安全な理由」が変わったので doc を書き直した
- ⚠️ **ネイティブバイナリは再ビルドしていない** —— 利用側 Unity へ届けるには
  `make build-macos` / `make build-ios` が要る

### [2026-09-09(セッション30)](history/10-2026-09-09.md)

- 🔴 **2026-09-10**(V14: 出力停止ウォッチドッグ。**通知が1つも来ないまま音声コールバックが
  止まる**ケースを塞いだ。`SE 発音16/失敗0/委譲0` なのに無音・再起動で直る、という
  テンプレート側の診断で確定。**原因を問わず「進んでいないなら直す」**にしたのが要点 ——
  通知を拾い足すやり方は R12 / R13 で2度やり直しており網羅が終わらない。ABI 変更なし)

- **2026-09-09**(D6: バックグラウンド移行時に `AVAudioSession` を解放する〈`setActive(false)` +
  **`NotifyOthersOnDeactivation`**〉。🔴 **出力を握り続けるリスクは元々無かった** ——
  本当の穴は「一度も非アクティブ化しておらず、**止めた相手の音楽が戻らない**」ことだった。
  `cfcb735`)

### [iOS 割り込み・ルート変化の調査記録(2026-08-28〜08-30)](history/09-2026-09-05.md)

- 🔴 **同じ症状(実機で音が復帰しない・Bluetooth の抜き差しで無音)が再発したらここから読む。**
  元は `ios_interruption.rs` のモジュール doc に「追記:」節として 400行まで積み上がっていたものを、
  2026-09-05 にここへ移した(リファクタ P2-7)。**モジュール doc 側には不変条件だけが残っている。**
  実機報告 R12 / R13 / R24 で当初の判断が2箇所否定された経緯を含む

### [2026-09-04(セッション21)](history/08-2026-09-04.md)

- **2026-09-04**(リファクタ Phase 1: FFI 層のボイラープレートを `guarded` / `send_command` へ集約〈P1-6〉+ `MusicTarget` で music/bgm の set・state・stop・set_loop を実体共有〈P1-7〉。🔴 公開 FFI の面が不変であることを csbindgen 生成物のバイト単位 diff で裏取り。state は命名差ではなく保存先が違うため統合せず振り分けのみ。`restore_music`/`restore_bgm` は意図的な非対称なので対象外。純減は実測 85行〈計画の「~400行」は P1-6 単体の見積もり〉)

### [2026-09-02](history/07-2026-09-02.md)

- **2026-09-04**(リファクタ Phase 0: `stop()` にも P0-4 と同じ「曲頭へ戻らない」欠陥が残っていたのを修正〈P0-4b〉。ネイティブ再ビルド + 実機聴感確認が必要。バイナリ3種は同日09:57〜09:58に再ビルド済み。📌 iOS clippy「pre-existing failure」の記述は誤りで exit code 0・CI 無影響と訂正)
- **2026-09-03 夜**(リファクタ Phase 0: 自然終了後の play() 無音を修正〈P0-4〉/ iOS 前面復帰のメインスレッド 370ms ブロックをワーカーへ委譲〈P0-6。実機未検証〉。🔴 stop() にも同種の欠陥が残存=P0-4b)

- **2026-09-02**(SE ボイスのループ再生を追加 —— ホールド保持音の「出力経路が違う」問題の一部を埋める。クライアント側の切り替えは別リポジトリの作業でスコープ外)

### [2026-08-31 〜(続き3)](history/06-2026-08-31.md)

- **2026-09-01**(R51-2: 予約再生中のポーズが効かずバックグラウンド復帰で鳴り出す —— `Ready` 状態の予約が破棄されていなかった)

- **2026-08-31**(MIT-0 で OSS ライセンスを整備 —— 第三者表記の自動生成 + 鮮度検査。`119c3ae`)
- **2026-08-31**(R30 の実機再確認 —— アプリが前面のままの Bluetooth 切断からの復帰が実機ログで確認できた)
- **2026-08-31**(Rust FFI 境界 ⇔ C# ラッパのズレ洗い出し + 自動検出の仕組み〈`csharp_abi_sync.rs`〉)

### [2026-08-31 〜(続き2)](history/05-2026-08-31.md)

- **2026-08-31**(middleware: M3 完了時の持ち越し項目のうち2つを解消 —— ①再オープンを
  諦めたことを既存の `Event::AudioInterruptionEnded` 転用で C# へ通知 /
  ②バックオフ値は `pub const` のままでよいと判断)

### [2026-08-31 〜(続き)](history/04-2026-08-31.md)

- **2026-08-31**(middleware: M3 完了 —— Android(AAudio)切断復旧、案A〔ミドルウェア内部で再オープン〕を実装)

### [2026-08-31 〜](history/03-2026-08-31.md)

- **2026-08-31**(middleware: M3 最後の残り —— Android(AAudio)切断復旧の設計調査。実装せず判断待ちで停止)
- **2026-08-31**(middleware: M4 終了条件「選曲プレビューのネイティブ化」—— 既存の `mw_music_*` がそのまま口になることを確認)

### [2026-08-29 〜 2026-08-30](history/02-2026-08-29--2026-08-30.md)

- **2026-08-31**(middleware: M3 アンダーラン検知・テレメトリ)
- **2026-08-30**(middleware: CI で4カウント落ちたフレークの真因と対策)
- **2026-08-30**(middleware: R24「`pause()`→`play()` が Ok でも無音」—— 復帰確認を実測に置き換え)
- **2026-08-29**(middleware: R12 の実機確認 + R13「Bluetooth 再接続で無音」)
- **2026-08-29**(middleware: M3 追撃その3 —— ホーム復帰で音が戻らない「安全網の前提が崩れていた」)
- **2026-08-29**(middleware: M4-3 BGM のネイティブ化)

### [2026-08-23 〜 2026-08-28](history/01-2026-08-23--2026-08-28.md)

- **2026-08-28**(middleware: M3 追撃 —— iOS 実機バグ修正その2「Bluetooth 解除で SE が無音になる」)
- **2026-08-28**(middleware: M3 着手 —— iOS 実機バグ修正「バックグラウンドから戻ると SE だけ無音になる」)
- **2026-08-28**(middleware: バグ修正 —— キャリブレーション画面キャンセルでメトロノームが鳴り続ける退行)
- **2026-08-24**〜25(middleware: M2-7 完了 —— 楽曲制御 API を FFI へ公開)
- **2026-08-23**(middleware: M1 完了 → M2 着手)
- **2026-08-23**(middleware: Android M1 計測に着手 —— 不具合3件を修正)

