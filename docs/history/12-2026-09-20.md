# 作業記録 2026-09-20

索引と、この記録全体の位置づけは [`../history.md`](../history.md) を見ること。

---

# 📝 ADR-0001(docs/adr/ 新設)+ ネイティブ再ビルド(P3-12 / P3-13 を Plugins へ反映)

- **ADR-0001**: セキュリティ枠組みの採用決定(2026-09-20 ユーザー決定。ワークスペース
  `docs/security/SECURITY-FRAMEWORKS.md` が正)のミドルウェアへの当てはめを記録し、
  `docs/adr/` を新設した。要点: Top 10 / ASVS はこのリポジトリの直接対象外。受け持ちは
  **サプライチェーン検査の継続**(clippy / cargo-deny)と**自前脅威モデルの
  「unsafe FFI・RT スレッド」**。`proptest` / `cargo-fuzz` / `miri` / `loom` が無い空白は
  認識した上で保留。
- **ネイティブ再ビルド**: P3-12 / P3-13(セッション42)以降、`unity/Runtime/Plugins/` を
  作り直していなかったのを解消。`make test`(cargo test)全緑 → `make build-macos` /
  `make build-ios`。バイナリは gitignore 対象なのでコミットは無い。利用側 client の
  スモーク(`NativeAudioIntegrationTest` 16/16 / `LiveScreenPreviewPlaybackTest` 9/9)も緑。
