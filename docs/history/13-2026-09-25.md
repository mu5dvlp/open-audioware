# 作業記録 2026-09-25

## 🔴 Dependabot PR#1 の原因を特定して修正

`dtolnay/rust-toolchain@<tag>` のタグは Rust の版そのものだった。Dependabot が存在しない
`1.120.0` へ上げたため、全ジョブが404になっていた。

`535172e` で `actions/checkout` と `upload-artifact` の v7 だけを取り込み、`rust-toolchain` は
Dependabot の対象外にした。PR#3 はユーザーがマージした(`cf076fe`)。PR#2(`symphonia` 0.6 /
`rubato` 5 など)は破壊的変更を含むため未着手。

## 🆕 Community Standards と文書・CI

`65a4c0d` で GitHub の Community Standards を **28% → 100%** にした。Private Vulnerability
Reporting を有効化し、説明文も設定した。README は敬体へ揃えた(`93fa89a`)。
共通 `ci-workflows` と mise の設定は `3fa2cda` で反映した。

