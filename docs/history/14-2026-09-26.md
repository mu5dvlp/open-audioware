# 作業記録 2026-09-26

## 🔴 Dependabot PR#2(依存の破壊的更新)を手で移植して取り込んだ

`e53b93e`。symphonia 0.5→0.6 / rubato 0.15→5.0 / rtrb 0.3→0.4 / cpal 0.18.1→0.18.2。移植は Codex(オフライン。
クレートは事前に取得)、レビューと検証はオーケストレータ。PR#2 は閉じた。

- symphonia 0.6: `AudioDecoder` / `CodecParameters::Audio` / 新しい probe・seek API。デコード済みサンプルは
  事前確保した `Vec<f32>` へコピー(伸ばすのはデコードスレッドだけ)
- rubato 5: 楽曲は `Fft` + `FixedSync::Both`、SE は `Async` sinc + `FixedAsync::Input`。末尾は
  `Indexing::partial_len`(0 = 全部無音でパディング。旧 API の `None` 入力に相当することをソースの doc で確認)
- 出力長・遅延(44.1k→48k で `output_delay=139`)は旧版と同じ。**補間値は rubato 5 の実装で変わる**
- 🔴 **mach2 は 0.6 に据え置き** —— cpal 0.18.2 が 0.6 のままで、0.7 にすると二重に入る
  (`mw-backend/Cargo.toml` の「cpal と同じ実装で `mach_timebase_info` を呼ぶ」前提も崩れる)。
  Dependabot の対象から外した(cpal が上げたら手で揃える)
- 音声コールバック側の変更なし。**334 passed** / `make lint` rc=0 / `make build-macos` `build-ios` 成功

⬜ **実機で音を確認する**(焼いた版で。補間値が変わったので、SE と楽曲の聞こえ方をユーザーに見てもらう)
